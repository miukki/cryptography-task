// @flow

import CryptographyTask from './'

let apiConfig = {}

describe('CryptographyTask', function() {
  describe('metho test', function() {
    it('method test', function(done) {
      //const cryptographyTask = new CryptographyTask(apiConfig)
      //CryptographyTask.someMethod({JSON.expired})
      // expect({}).toBeTruthy()
      done()
    })
  })
})
